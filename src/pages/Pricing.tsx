import PageTemplate from "@/components/PageTemplate";

const Pricing = () => {
  return (
    <PageTemplate 
      subtitle="Economics // 04"
      title="Resource Tiers"
      description="Flexible infrastructure pricing designed to scale from independent research to global enterprise deployment."
    />
  );
};

export default Pricing;
