import PageTemplate from "@/components/PageTemplate";

const Products = () => {
  return (
    <PageTemplate 
      subtitle="The Suite // 01"
      title="Our Products"
      description="From autonomous engineering environments to global-scale deployment systems, explore the tools built for the next decade of infrastructure."
    />
  );
};

export default Products;
